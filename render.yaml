services:
  - type: web
    name: wedding-invitation
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: FLASK_ENV
        value: production